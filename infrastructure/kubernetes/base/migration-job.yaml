apiVersion: batch/v1
kind: Job
metadata:
  name: database-migration
  namespace: zoryqon
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        app: zoryqon-database-migration
    spec:
      restartPolicy: Never
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: migration
          image: registry.example.invalid/zoryqon/database:replace-with-signed-digest
          imagePullPolicy: IfNotPresent
          command: ['npm', 'run', 'migrate', '-w', '@zoryqon/database']
          envFrom:
            - secretRef:
                name: zoryqon-database-runtime
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ['ALL']
