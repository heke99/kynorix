import { InfoPage } from '../../components/InfoPage';
export default function SupportPage() {
  return (
    <InfoPage eyebrow="Customer support" title="How can we help?">
      <p>
        Contact verified Zoryqon support from your account. Never send passwords, MFA secrets,
        private keys or full identity documents in a support message.
      </p>
    </InfoPage>
  );
}
